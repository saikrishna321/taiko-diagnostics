{
  "name": "taiko-diagnostics",
  "version": "1.0.0",
  "description": "A plugin for taiko which provides some diagnostics features",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "speedline": "^1.4.2",
    "taiko-plugin": "file:../taiko-plugin"
  }
}
