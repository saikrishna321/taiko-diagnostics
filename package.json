{
  "name": "taiko-diagnostics",
  "version": "0.0.1",
  "description": "A plugin for taiko which provides some diagnostics features",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc"
  },
  "author": "BugDiver",
  "license": "MIT",
  "dependencies": {
    "speedline": "^1.4.2"
  },
  "devDependencies": {
    "ts-node": "^8.0.2",
    "typescript": "^3.3.3333"
  }
}
